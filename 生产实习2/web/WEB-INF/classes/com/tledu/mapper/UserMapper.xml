<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tledu.mapper.UserMapper">
<select id="checkUserNameUniqu" resultType="User">
    select * from t_user where username=#{username}
</select>
    <insert id="add" parameterType="User">
        insert  into t_user(username,password,email) value (#{username},#{password},#{email})
    </insert>
    <select id="grtUserByUserName" parameterType="User" resultType="User">
        select * from t_user where username=#{username}
    </select>
</mapper>